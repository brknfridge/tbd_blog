// Global site metadata. `url` is filled automatically by Netlify at build time
// (the URL env var = your production domain). Locally it falls back to localhost.
// Update the fallback if you deploy somewhere other than Netlify.
module.exports = {
  url: process.env.URL || "http://localhost:3000",
  name: "[tbd] Dispatches",
  publisher: "[tbd]",
  description:
    "A growth journal — precise, confident, in motion. Essays on marketing that compounds.",
};
